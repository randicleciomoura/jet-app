# Backend Source

Pasta raiz do código da API.

## Padrão de pastas

- `modules/` para módulos NestJS.
- `common/` para componentes reutilizáveis do backend.
- `config/` para variáveis e configurações.
- `types/` para tipos locais.

A implementação será adicionada por etapas pequenas e isoladas.
